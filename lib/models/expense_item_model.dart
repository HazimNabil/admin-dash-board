class ExpenseItemModel {
  final String icon, title, date, price;

  const ExpenseItemModel({
    required this.icon,
    required this.title,
    required this.date,
    required this.price,
  });
}
